# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'circleci_test' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for circleci_test

  target 'circleci_testTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'circleci_testUITests' do
    # Pods for testing
  end

end
